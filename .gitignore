# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Understand-Anything knowledge graph (regenerated on demand)
.understand-anything/
.ua/

# impeccable working files, kept on disk only
.impeccable/

# Design and product records, kept on disk only. The tokens they describe
# ship in src/styles/theme.css, which is what the app actually reads.
DESIGN.md
PRODUCT.md

# Local tooling, deliberately not shared: Claude settings, vendored skills,
# and CI workflows are kept on disk only.
.claude/
.github/

# Photo originals, kept out of the repo (only the processed renditions ship)
photos-source/
